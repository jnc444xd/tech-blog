{{#each posts as |post|}}
<div class="card mt-4 mb-4">
    <a href="/comment/{{post.id}}" class="card-header"><h2>{{post.name}}</h2></a>
    <span class="mx-4">{{post.text}}</span><br>
    <span class="mx-4">Posted by {{post.user.name}} on {{format_date post.createdAt}}</span><br>
</div>
{{/each}}